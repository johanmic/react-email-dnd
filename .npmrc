# pnpm automatically converts workspace:^ to ^x.y.z during publish
# No special config needed - this is built-in behavior

# Hoist patterns for better compatibility
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
